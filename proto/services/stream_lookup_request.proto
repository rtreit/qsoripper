syntax = "proto3";

package qsoripper.services;

option csharp_namespace = "QsoRipper.Services";

message StreamLookupRequest {
  string callsign = 1;
  bool skip_cache = 2;  // Force fresh lookup, bypass L1 cache
}
