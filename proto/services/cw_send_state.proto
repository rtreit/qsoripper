syntax = "proto3";

package qsoripper.services;

option csharp_namespace = "QsoRipper.Services";

enum CwSendState {
  CW_SEND_STATE_UNSPECIFIED = 0;
  CW_SEND_STATE_ACCEPTED = 1;
  CW_SEND_STATE_REJECTED = 2;
  CW_SEND_STATE_ABORT_REQUESTED = 3;
  CW_SEND_STATE_COMPLETED = 4;
}
