syntax = "proto3";

package qsoripper.services;

option csharp_namespace = "QsoRipper.Services";

// The CAT hub `[cat_hub.events]` section: native push policy.
message CatHubEventSettings {
  // Whether to enable the radio's native push stream.
  optional bool native_push = 1;
}
